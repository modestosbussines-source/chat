# Omni - Docker Environment
# Copie para .env e configure

# Timezone
TZ=UTC

# PostgreSQL
POSTGRES_USER=omni
POSTGRES_PASSWORD=CHANGE_THIS_STRONG_PASSWORD
POSTGRES_DB=omni

# Redis (opcional, para autenticação)
REDIS_PASSWORD=CHANGE_THIS_REDIS_PASSWORD
